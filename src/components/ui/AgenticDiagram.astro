<div class="relative mx-auto max-w-3xl">
  <!-- Section labels -->
  <div class="grid grid-cols-2 text-sm text-neutral-500 mb-4">
    <span>Etapas determinísticas</span>
    <span class="text-right">Razonamiento agentic</span>
  </div>

  <div class="grid grid-cols-2 gap-12">
    <!-- Left column: deterministic -->
    <div class="space-y-4 relative">
      <div class="pipeline-box bg-neutral-50">Ingestión</div>
      <div class="pipeline-box bg-neutral-50">OCR</div>
      <div class="pipeline-box bg-neutral-50">Layout y orden de lectura</div>
      <div class="pipeline-box bg-neutral-50">Comprensión del documento</div>

      <!-- vertical line -->
      <div
        class="absolute left-1/2 top-12 bottom-12 w-px bg-neutral-300 -translate-x-1/2"
      >
      </div>
    </div>

    <!-- Right column: agentic -->
    <div class="space-y-4 relative">
      <div class="pipeline-box border-indigo-500 bg-indigo-50">
        <div class="font-medium">Extracción agentic</div>
        <div class="mt-1 text-xs text-indigo-600">
          plan · extraer · validar · reintentar
        </div>
      </div>

      <div class="pipeline-box">Salida estructurada (JSON)</div>
      <div class="pipeline-box">Artefactos para RAG</div>

      <!-- loop -->
      <div class="absolute -top-6 right-2 text-xs text-neutral-400">
        ↺ validación
      </div>
    </div>
  </div>
</div>
